import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Forced configuration reload to clear Next.js App Router 404 cache bug
};

export default nextConfig;
